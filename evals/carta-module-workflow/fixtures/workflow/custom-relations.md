# Approved module fixture

Create a new `service-requests` module. List, Create and Update use independent
scalar fields `title` and `active` with system permissions. Detail displays a
related owner and owned child notes. Child writes use project scope. The custom
Detail browser path must prove these rules. No framework change is approved.
No destination exists for this module.
