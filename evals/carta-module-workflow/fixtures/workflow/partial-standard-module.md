# Approved module fixture

Create a new scalar `service-levels` resource with text UUID `id`, text `name`
and boolean `active`. System permissions cover List, Create and Update. Detail is
a custom page with one read-only status section. List is the form redirect target.
The `settings` navigation group exists. API, web, test and browser are required.
No source or migration destination exists for this module.
