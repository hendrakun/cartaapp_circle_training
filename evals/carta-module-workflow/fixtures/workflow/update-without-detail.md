# Approved module fixture

Create a new scalar `service-levels` resource with List and Update only. It has
text UUID `id`, text `name`, system permissions and the existing `settings`
navigation group. There is no Detail action, page, link or permission. The edit
form must load the record and return to List after a successful update. API, web,
test and browser are required. No destination exists for this module.
