{
  "schemaVersion": 1,
  "evaluationStatus": "PARTIAL",
  "cases": [
    {
      "id": "discovery-process",
      "stage": "design",
      "request": "Design a purchase-request module. Our approval process is not documented in this brief. Start by understanding how our team actually uses it.",
      "inputs": []
    },
    {
      "id": "conflicting-references",
      "stage": "design",
      "request": "Design purchase-request approvals using these two real reference documents. Preserve established decisions, but resolve anything that would change the new behavior.",
      "inputs": [
        "fixtures/sources/approval-procedure.md",
        "fixtures/sources/completed-request.md"
      ]
    },
    {
      "id": "subsequent-module",
      "stage": "design",
      "request": "Add category selection to service requests using our existing category data. The attached owner describes the current subsystem. Establish the contract before planning.",
      "inputs": [
        "fixtures/owners/categories.ts"
      ]
    },
    {
      "id": "approved-plan-handoff",
      "stage": "plan",
      "request": "I approve revision r1 as attached. Write the Carta implementation plans; retain improve-style plan structure. Do not implement.",
      "inputs": [
        "fixtures/approved/design.md",
        "fixtures/owners/categories.ts"
      ]
    },
    {
      "id": "partial-standard-module",
      "stage": "execute",
      "request": "Run `pnpm scaffold:bounded-module -- --manifest evals/carta-module-workflow/fixtures/workflow/partial-standard-module.json --check --json` and report the observed result. State what the generator creates and what stays manual from the approved design. Do not apply changes.",
      "inputs": ["fixtures/workflow/partial-standard-module.md", "fixtures/workflow/partial-standard-module.json"]
    },
    {
      "id": "update-without-detail",
      "stage": "execute",
      "request": "Run `pnpm scaffold:bounded-module -- --manifest evals/carta-module-workflow/fixtures/workflow/update-without-detail.json --check --json` and report the observed result. State the generated dependency and browser redirect. Do not apply changes.",
      "inputs": ["fixtures/workflow/update-without-detail.md", "fixtures/workflow/update-without-detail.json"]
    },
    {
      "id": "read-only-seeded-module",
      "stage": "execute",
      "request": "Run `pnpm scaffold:bounded-module -- --manifest evals/carta-module-workflow/fixtures/workflow/read-only-seeded-module.json --check --json` and report the observed result. State the generated outputs and proof. Do not apply a migration or run a seed.",
      "inputs": ["fixtures/workflow/read-only-seeded-module.md", "fixtures/workflow/read-only-seeded-module.json"]
    },
    {
      "id": "custom-relations",
      "stage": "execute",
      "request": "Run `pnpm scaffold:bounded-module -- --manifest evals/carta-module-workflow/fixtures/workflow/custom-relations.json --check --json` and report the observed result. Separate the compatible generated work from the manual work in the approved design. Do not change a framework package.",
      "inputs": ["fixtures/workflow/custom-relations.md", "fixtures/workflow/custom-relations.json"]
    },
    {
      "id": "environment-failure",
      "stage": "execute",
      "request": "Run `pnpm module:preflight -- --needs browser`. Report the exact failed capability and correction from the observed result. Then run the partial-standard-module read-only manifest check and report what independent work can continue. Do not apply changes.",
      "inputs": ["fixtures/workflow/partial-standard-module.md", "fixtures/workflow/partial-standard-module.json"]
    },
    {
      "id": "migration-contamination",
      "stage": "execute",
      "request": "Run `node --test --test-name-pattern='bounded --apply orchestrates migration with rollback on unrelated operations' scripts/scaffold-bounded-module.test.mjs`. Inspect the isolated failure-path assertions and report the observed safe result. Do not change product source.",
      "inputs": ["fixtures/workflow/read-only-seeded-module.md"]
    },
    {
      "id": "resume-implemented-plan",
      "stage": "resume",
      "request": "Continue the approved feature from its current worksheet and completed implementation. Do not restart discovery. Verify the implemented scope before working on any remaining plan.",
      "inputs": [
        "fixtures/approved/design.md"
      ],
      "preparation": "Use the actual artifacts produced by approved-plan-handoff and its implementation run. If those were not run, mark this case NOT_RUN; do not invent a completed worksheet."
    },
    {
      "id": "review-semantic-defects",
      "stage": "verify",
      "request": "Review this existing category list implementation against the approved contract. The UI hides navigation from users without list permission. Give a scoped source-review verdict; no runtime is provided for this fixture.",
      "inputs": [
        "fixtures/approved/design.md",
        "fixtures/review/category-route.ts"
      ]
    },
    {
      "id": "stale-same-path-evidence",
      "stage": "verify",
      "request": "Review the implemented plan and its previous successful report. The relevant source may have changed since that report. Reuse only current sufficient evidence.",
      "inputs": [],
      "preparation": "Use a real implemented case and real command report; change an input file without renaming it. Preserve the pre-change report. Do not hand-write success evidence."
    },
    {
      "id": "runtime-unavailable",
      "stage": "verify",
      "request": "Finish verification of the approved UI feature using the available checks. Identify precisely what remains unverified.",
      "inputs": [
        "fixtures/approved/design.md"
      ],
      "preparation": "Use the actual implementation run but disable required browser/database capability in the harness. Record which capability is unavailable. Static tests must not be represented as runtime success."
    },
    {
      "id": "material-gap-in-approved-draft",
      "stage": "plan",
      "request": "Plan this approved draft for purchasing approvals: managers approve requests and Finance can reject after approval. Retrying an approval must be supported. The draft does not define what happens if approval and rejection happen together. Resolve only what prevents a sound plan.",
      "inputs": [
        "fixtures/sources/approval-procedure.md"
      ]
    }
  ]
}
